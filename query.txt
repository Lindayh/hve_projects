-- database: c:\Users\Test\Desktop\AIDEV23S_UW_Inlamn02\instance\personalregister.db

-- Use the ▷ button in the top right corner to run the entire file.

CREATE TABLE `user_tmp` (
	id INTEGER NOT NULL, 
	username VARCHAR, 
	password VARCHAR, 
	roles VARCHAR, 
	active BOOLEAN, 
	fs_uniquifier VARCHAR(255) NOT NULL, 
	PRIMARY KEY (id), 
	UNIQUE (fs_uniquifier), 
	UNIQUE (username),
	FOREIGN KEY (roles) REFERENCES role(id)
);

DROP TABLE user
